telethon==1.44.0
google-genai==1.75.0
psycopg2-binary==2.9.12
fastapi==0.141.1
uvicorn==0.52.1
python-dotenv==1.2.2
requests>=2.32.4
slowapi==0.1.10

# numpy is pinned deliberately. It used to be `numpy>=1.24.0`, which resolves to
# 2.x against a torch and pandas compiled for 1.x — any clean rebuild would have
# failed at import with an ABI error ("compiled using NumPy 1.x ... run in 2.x").
numpy==2.5.1
pandas==2.3.3

# 1.3.0 wheels bundle prophet_model.bin and cmdstan 2.37.0, so the Dockerfile no
# longer needs to install cmdstan by hand before this.
prophet==1.3.0

# CPU-only torch. Plain PyPI wheels drag in ~2.5GB of CUDA packages this host
# cannot use; the +cpu local version keeps resolution on the CPU index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
sentence-transformers==5.6.1

# Pinned on purpose, not incidentally. sentence-transformers 2.2.2 imported
# `cached_download` from huggingface-hub, which was removed upstream; because
# these were unpinned the dedup model silently stopped loading and fell back to
# Jaccard. Keeping them explicit means that failure mode cannot recur unnoticed.
transformers==5.14.1
huggingface-hub==1.26.0
