# 🗄️ DATABASE (Supabase)
INTERNAL_STORE_URI=postgresql://postgres:password@db_host:5432/postgres

# 🛡️ API SECURITY (CORS)
# REQUIRED: Set your exact frontend domain(s). Do NOT use wildcard '*' in production.
ALLOWED_ORIGINS=https://burmaduta.com,https://burmaduta.pages.dev

# 🤖 AI Configuration (stored in DB, but can be overridden here)
# MODEL_NAME=gemini-2.5-flash-lite
# AI_PROMPT=

# 📡 Telegram Scraper (stored in DB via system_config table)
# SOURCE_API_ID=
# SOURCE_API_HASH=
# SOURCE_CHANNELS=
# PROCESSOR_KEY=
